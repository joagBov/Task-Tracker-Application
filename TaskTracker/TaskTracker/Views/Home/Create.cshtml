<!--
        FILE          : Create.cshtml
        PROJECT       : Task Tracker Application
        PROGRAMMER(S) : Mehakpreet Singh, Elijah Atta-Konadu, Navjot Singh Bhullar, Chase McCash
        FIRST VERSION : 2025
        DESCRIPTION   : Razor view for creating a new task in the Task Tracker application.
-->
@{
    ViewData["Title"] = "Create Task";
}
<div class="container">
    <h1>Create New Task</h1>
    <div class="row">
        <div class="col-md-6">
            <form asp-action="Create" method="post">
                <div class="mb-3">
                    <label for="title" class="form-label">Title *</label>
                    <input type="text" class="form-control" id="title" name="title" required>
                    @if (ViewData.ModelState["Title"]?.Errors.Count > 0)
                    {
                        <div class="text-danger">@ViewData.ModelState["Title"]?.Errors[0]?.ErrorMessage</div>
                    }
                </div>
                <div class="mb-3">
                    <label for="description" class="form-label">Description</label>
                    <textarea class="form-control" id="description" name="description" rows="4"></textarea>
                </div>
                <button type="submit" class="btn btn-primary">Create Task</button>
                <a href="@Url.Action("Index")" class="btn btn-secondary">Cancel</a>
            </form>
        </div>
    </div>
</div>